# 10. Framework 생성하기

<div align="left"><figure><img src="../.gitbook/assets/image (2) (2).png" alt=""><figcaption></figcaption></figure></div>

스파이더젠에서 프레임워크를 생성하는 매뉴얼

## Example

<figure><img src="../.gitbook/assets/image (3) (2).png" alt=""><figcaption></figcaption></figure>

* File > New Propect > Project Type: FrameworkGen으로 지정 > Project Name 설정(ex: newComp)

***

<div align="left"><figure><img src="../.gitbook/assets/image (5) (2).png" alt=""><figcaption></figcaption></figure></div>

* &#x20;Source > newComp 폴더에서 우클릭 > Add Component

***

<div align="left"><figure><img src="../.gitbook/assets/image (1) (1).png" alt=""><figcaption></figcaption></figure> <figure><img src="../.gitbook/assets/스크린샷 2025-07-14 131637.png" alt=""><figcaption></figcaption></figure></div>

* 새로 생성할 컴포넌트의 Name 지정하고 Base Class에서 상속받을 컴포넌트 선택 > OK

***

<div align="left"><figure><img src="../.gitbook/assets/image (1) (1) (2).png" alt=""><figcaption></figcaption></figure></div>

* 폴더에 신규 컴포넌트를 위한 파일들 자동 생성

***

* 컴포넌트 이미지 변경&#x20;

<div><figure><img src="../.gitbook/assets/image (140).png" alt=""><figcaption></figcaption></figure> <figure><img src="../.gitbook/assets/스크린샷 2025-07-15 114357.png" alt=""><figcaption></figcaption></figure></div>

* image > comp 우클릭> Add existing files 클릭
* jpg, png 등 이미지파일 선택하고 더블 클릭
* 선택하신 외부파일 복사? > Yes
* 프로젝트명.frw 파일 > 상단에 버전 추가하고 프로젝트 build

| **파일**                    | **설명**                                                                                     |
| ------------------------- | ------------------------------------------------------------------------------------------ |
| `NewButtonAttrProp.js`    | 컴포넌트의 정보를 표현하고 속성창에서 입력한 값을 컴포넌트에 적용하는 클래스                                                 |
| `NewButtonAttrItems.attr` | 속성 UI를 정의하는 JSON 데이터                                                                       |
| `NewButton.js`            | 생성한 컴포넌트 클래스. 컴포넌트에서 제공하는 기능이나 함수를 정의.                                                     |
| `NewButtonEvent.js`       | 생성한 컴포넌트의 이벤트 클래스. 컴포넌트에서 제공하는 이벤트를 정의.                                                    |
| `NewFrw.js`               | 프레임워크에서 제공하는 라이브러리. 함수나 변수 및 컴포넌트에 대한 표시용 라벨을 정의 (lay 파일 내에서 "NewButton"이라는 이름으로 표시되도록 설정) |
| `NewFrw.frw`              | 프레임워크에서 제공하는 컴포넌트 이벤트 라이브러리 레이아웃 이미지 등을 작성하는 패키지 설정 파일                                     |
| `comp.css`                | 생성한 컴포넌트들의 스타일을 정의하는 파일                                                                    |

***

#### 1. `SET_VALUE` : 사용자 정의 속성 값을 내부 메서드로 전달(get, set 함수 필요)

* AttrItems.attr

```javascript
{
    name: 'ATextField',
    id: 'Text',
    group: 'SET_VALUE',
    event: 'change',
    listener: 'onTextValueChange',
    style: { width: '60%', height: '100%' }
}
```



* newbutton.js(get, settext 필요)

```javascript
(async function()
{

await afc.import("Framework/afc/component/AButton.js")

class NewButton extends AButton
{
    static CONTEXT = 
    {
        tag:'<button data-base="NewButton" data-class="NewButton" class="AButton-Style NewButton-Style">Button</button>',
        defStyle: { width:'80px', height:'22px' },
        events: ['click', 'longtab', 'myclick']
    }

    constructor()
    {
        super();
        this.frwName = 'newComp';
    }

    init(context, evtListener)
    {
        super.init(context, evtListener);
    }

    // 버튼 텍스트 설정
    setText(text)
    {
        this.element.innerText = text;
    }

    // 버튼 텍스트 가져오기
    getText()
    {
        return this.element.innerText;
    }
}

})();
```



* AttrProp.js

<pre class="language-js"><code class="lang-js">// 컴포넌트 속성창에서 입력한 값을 컴포넌트에 적용
applyValueToSelComp(selComp, dataKey, valGroup, value) {
    if (valGroup === 'SET_VALUE') {

        if (dataKey === 'Text') {
            selComp.setTitle(value);  // 버튼 텍스트 설정(gettitle, gettext)
            return;
        }

        //  기본   동작으로 처리됨 
        //      else if (dataKey === 'Image') {
        //          selComp.setImage(value);  // 아이콘 이미지 설정
        //            return;
<strong>        //        }
</strong>
        else if (dataKey === 'IconMargin') {
            // 마진은 보통 문자열로 들어옴: "5px 10px 5px 10px"
            selComp.setOption({ iconMargin: value }); -> getIconMargin 생성 -> option.iconmargin 
            return;
        }

        else if (dataKey === 'IconSize') {
            // 사이즈도 문자열: "30px 100%" 등
            selComp.setOption({ iconSize: value });
            return;
        }
    }

    return super.applyValueToSelComp(selComp, dataKey, valGroup, value);
}

// 컴포넌트에 적용된 값을 속성창에 표현
getUpdateValue(selComp, dataKey, valGroup) {
    if (valGroup === 'SET_VALUE') {

        if (dataKey === 'Text') {
            return selComp.getTitle();
        }

        else if (dataKey === 'IconMargin') {
            return selComp.getIconMargin();
        }

        else if (dataKey === 'IconSize') {
            return selComp.getIconSize();
        }
    }

    return super.getUpdateValue(selComp, dataKey, valGroup);
}
</code></pre>

***

#### 2. `ATTR_VALUE` : DOM에 attribute를 변경하는 경우에 사용(속성값 적용, 속성값 가져옴)

```javascript
{
    name: 'ACheckBox',
    text: 'External Link',
    id: 'data-aftertext',
    group: 'ATTR_VALUE',
    event: 'click',
    listener: 'onCheckBtnClick'
}
```

```js
applyValueToSelComp(selComp, dataKey, valGroup, value) {
    if (valGroup === 'ATTR_VALUE') {

        if (dataKey === 'data-aftertext') {
            selComp.setOption({ imgAfterText: value });

            selComp.refreshAfterText?.();
            return;
        }

        else if (dataKey === 'data-newline') {
            selComp.setOption({ imgNewLine: value });

            selComp.setImage(selComp.getImage());
            return;
        }

        else if (dataKey === 'data-off-downstate') {
            selComp.setOption({ autoDownState: !value }); // 컴포넌트의 메모리에 저장

            // 상태 리셋 또는 클래스 토글 등 눈에 띄게
            if (value) selComp.element.classList.add('disable-down');
            else selComp.element.classList.remove('disable-down');

            return;
        }
    }

    return super.applyValueToSelComp(selComp, dataKey, valGroup, value);
}
```

***

#### 3. `CSS_VALUE` : 사용자가 선택한 값을 **해당 컴포넌트의 CSS 스타일 속성으로 직접 적용**

```javascript
{
    name: 'ASelectBox',
    id: 'font-size',
    group: 'CSS_VALUE',
    event: 'change',
    listener: 'onSelectValueChange',
    items: [ ['12px', '12px'], ['14px', '14px'], ['16px', '16px'] ]
}
```

***

| 그룹           | 적용 예시                  | 처리 방식                                                                                                                                                                                                                |
| ------------ | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SET_VALUE`  | 텍스트 입력 필드 (`Title`)    | <p><code>selComp.setText(value)</code></p><p><code>selComp.setImage(value)</code></p><p><code>selComp.setTitle(value)</code></p><p><code>selComp.setValue(value)</code></p><p><code>selComp.setUrl(value)</code></p> |
| `ATTR_VALUE` | 체크박스 (`data-external`) | <p><code>selComp.setAttr(dataKey, value)</code></p><p><code>setOption({ key: value })</code></p><p><code>setImage(selComp.getImage())</code></p>                                                                     |
| `CSS_VALUE`  | 드롭다운 (`font-size`)     | DOM에 직접 반영                                                                                                                                                                                                           |

***

***

<div align="left"><figure><img src="../.gitbook/assets/image (138).png" alt=""><figcaption></figcaption></figure></div>



* 프로젝트 폴더에서 Source > newComp > 폴더명 newComp-0.0.1로 변경 > 복사

***

<figure><img src="../.gitbook/assets/image (136).png" alt=""><figcaption></figcaption></figure>

***



* 복사한 폴더를 아래 경로에 붙여넣기
  * SpiderGen5\resources\app\bin\frws\\

<div align="left"><figure><img src="../.gitbook/assets/image (137).png" alt=""><figcaption></figcaption></figure></div>

***



* Framework 우클릭 > Add Framework > newComp-0.0.1 선택

<div align="left"><figure><img src="../.gitbook/assets/image (2) (2).png" alt=""><figcaption></figcaption></figure> <figure><img src="../.gitbook/assets/스크린샷 2025-07-15 133438.png" alt=""><figcaption></figcaption></figure></div>

***



* Component에Framework 생성

<div align="left"><figure><img src="../.gitbook/assets/스크린샷 2025-07-15 095547.png" alt=""><figcaption></figcaption></figure></div>

***



* 컴포넌트 배치(속성창 같이)

<figure><img src="../.gitbook/assets/스크린샷 2025-07-15 095725.png" alt=""><figcaption></figcaption></figure>

